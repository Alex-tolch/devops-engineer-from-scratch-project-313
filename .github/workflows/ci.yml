name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  test-and-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: "app/pyproject.toml"

      - name: Set up Python
        run: cd app && uv python install

      - name: Install dependencies
        run: cd app && uv sync

      - name: Run tests
        run: cd app && uv run pytest

      - name: Run Ruff
        run: |
          cd app && uv run ruff check .
          cd app && uv run ruff format --check .
